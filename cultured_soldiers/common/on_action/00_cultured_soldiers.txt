on_legion_raised = {
  effect = {
    random_legion_unit = {
			unit_location.governorship = { # Do I need to scope to a unit?
        save_scope_as = this_gov
				set_variable = {
					name = local_pops_of_country_culture
					value = 0
				}
				set_variable = {
					name = local_pops_of_other_cultures
					value = 0
				}

				every_governorship_state = {
					every_state_province = {
						every_pops_in_province = {
							limit = { # Get pops of country culture group
								culture_group = root.owner.culture_group
							}
							scope:this_gov = { # Add to total of pops of country's culture group in the whole state
								change_variable = {
									name = local_pops_of_country_culture
									add = 1
								}
							}
						}
						every_pops_in_province = {
							limit = { # Get pops of other culture groups
								NOT = { culture_group = root.owner.culture_group }
							}
							scope:this_gov = { # Add to total of pops of other cultures group in the whole state
								change_variable = {
									name = local_pops_of_other_cultures
									add = 1
								}
							}
						}
					}
				}

				set_variable = { # Get ratio of country culture group pops here
					name = local_country_culture_ratio
					value = VAR:local_pops_of_country_culture
				}
				change_variable = { # If >1, then it is majority country culture
					name = local_country_culture_ratio
					divide = VAR:local_pops_of_other_cultures
				}
			}
		}
  }
}
